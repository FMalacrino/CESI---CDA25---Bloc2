<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Security.Mobile.Views.ResourceListView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="clr-namespace:HorusStudio.Maui.MaterialDesignControls;assembly=HorusStudio.Maui.MaterialDesignControls"
    xmlns:mi="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodels="clr-namespace:Security.Mobile.ViewModels"
    x:Name="page"
    Title="Ressources"
    BackgroundColor="{x:Static material:MaterialDarkTheme.Surface}">

    <ContentPage.BindingContext>
        <viewmodels:ResourceListViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            BindingContext="{Binding Source={x:Reference root}, Path=BindingContext}"
            Command="{Binding RefreshCommand}"
            EventName="Appearing" />
    </ContentPage.Behaviors>

    <Grid x:Name="root">
        <ScrollView Margin="16">
            <CollectionView ItemsSource="{Binding Items}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid
                            Margin="0,4"
                            ColumnDefinitions="*,Auto">
                            <Label
                                Grid.Column="0"
                                Text="{Binding Name}"
                                VerticalOptions="Center" />
                            <material:MaterialIconButton
                                Grid.Column="1"
                                BackgroundColor="Transparent"
                                Command="{Binding Source={x:Reference root}, Path=BindingContext.ToggleFavoriteCommand}"
                                CommandParameter="{Binding .}"
                                Type="Standard">
                                <material:MaterialIconButton.ImageSource>
                                    <FontImageSource
                                        FontFamily="{Binding IsFavorite, Converter={StaticResource BoolToButtonTypeConverter}}"
                                        Glyph="{x:Static mi:MaterialSharp.Favorite}" />
                                </material:MaterialIconButton.ImageSource>
                            </material:MaterialIconButton>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>
        <material:MaterialIconButton
            Margin="48,32"
            CornerRadius="16"
            HeightRequest="64"
            HorizontalOptions="End"
            Type="Filled"
            VerticalOptions="End"
            WidthRequest="64">
            <material:MaterialIconButton.ImageSource>
                <FontImageSource
                    FontFamily="MaterialOutlinedFilled"
                    Glyph="{x:Static mi:MaterialSharp.Add}" />
            </material:MaterialIconButton.ImageSource>
        </material:MaterialIconButton>
    </Grid>
</ContentPage>